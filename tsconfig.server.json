{   
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "outDir": "./dist",                              /* Redirect output structure to the directory. */
        "rootDir": "./src",
    },    
    "include": [
        "src/**/*.ts"
        ],
    "exclude": [
        "node_modules"
    ]
}